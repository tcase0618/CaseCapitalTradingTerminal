# Pre-Fable Frontend Snapshot

This folder preserves the frontend as it existed before the v6 institutional UI overhaul.

- Source snapshot: `src/`
- Package snapshot: `package.json`
- Captured on: 2026-08-02
- Reason: keep the old frontend code available in-repo so the terminal can be compared or restored without hunting through Git history.

The active app does not compile this folder. The production frontend still builds from `frontend/src`.
