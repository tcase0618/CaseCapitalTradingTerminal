# Use this for separate local frontend/backend dev servers.
REACT_APP_BACKEND_URL=http://127.0.0.1:8001

# For Docker/nginx or same-domain cloud deployments, leave it empty so the app uses /api.
# REACT_APP_BACKEND_URL=
