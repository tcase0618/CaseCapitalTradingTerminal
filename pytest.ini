[pytest]
markers =
    live_destructive: tests that can cancel orders, close positions, or trigger live/paper execution
    external_integration: tests that require an explicitly configured HTTP service
    postgres_integration: tests that require an explicitly configured isolated Postgres database
asyncio_mode = auto
addopts = -m "not live_destructive and not external_integration and not postgres_integration"
